import { LoginForm } from "@/components/login-form";

const Login = () => {
  return (
    <div className="flex justify-center items-center w-full h-[100dvh] min-h-screen p-4">
      <LoginForm />
    </div>
  );
};

export default Login;
